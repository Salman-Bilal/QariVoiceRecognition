# ─────────────────────────────────────────────
# Backend-only requirements
# Used by: api/main.py, api/audio_validator.py, matching/similarity.py
# ─────────────────────────────────────────────

# API server
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# Audio loading & validation (audio_validator.py)
librosa==0.10.2
soundfile==0.12.1

# Speaker embedding inference (matching/similarity.py)
torch==2.3.1
torchaudio==2.3.1
speechbrain==1.0.0

# Numerical ops (used across all modules)
numpy==1.26.4
